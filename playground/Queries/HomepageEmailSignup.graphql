query homepageEmailSignup($sitecorePath: String, $version: Int) {
  homepageEmailSignup: item(path: $sitecorePath, version: $version) {
    id
    name
    components: children {
      id
      template {
        name
      }
    }
  }
}